const {ethers} = require("hardhat");

describe("Payment contract", function() {
    let makePaymentContract;
    it("",async function(){
        const makePaymentContract = await ethers.deployContract("Payment",[]);
        
    })
})